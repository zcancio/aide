{
  "name": "aide-engine",
  "version": "1.0.0",
  "description": "AIde Kernel - Pure function engine for state management and rendering",
  "main": "builds/engine.js",
  "types": "builds/engine.ts",
  "exports": {
    ".": {
      "import": "./builds/engine.js",
      "types": "./builds/engine.ts"
    }
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "typecheck": "tsc --noEmit",
    "smoke": "node tests/smoke.test.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
