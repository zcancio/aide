Build an aide page using the aide-builder skill.

## Instructions

1. Read `skills/aide-builder/SKILL.md` thoroughly before starting
2. If the user provided an existing aide HTML file, operate in **reducer mode** (apply changes to existing state)
3. If starting from scratch, operate in **creation mode** (build from natural language description)
4. Follow every step in the skill — state design, event log, snapshot, blueprint, render
5. Save the output HTML to the current working directory

## Arguments

$ARGUMENTS

If no arguments provided, ask the user: "What are you running?"
