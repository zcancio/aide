# Ship It

Read `.claude/skills/spec-to-merge/SKILL.md` and execute **Phase 2** only.

The user has already tested the branch manually. Now: run local checks (lint, format, security, tests), push any fixes, and enable auto-merge. Do NOT re-implement anything — just validate and ship.

If no PR number is provided, check the current branch and find its open PR.

PR number: $ARGUMENTS
